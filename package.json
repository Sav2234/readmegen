{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Application that allows the user to create and README for a GitHub project by using input",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Savion Elsey-Williams",
  "license": "ISC",
  "dependencies": {
  "inquirer": "^6.5.2"
  }
}
